name: Call commitlint
on:
  pull_request_target:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check_job:
    uses: peeweep/.github/.github/workflows/commitlint.yml@backport-call-commintlint
